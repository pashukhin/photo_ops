import { ClusterView } from '../../components/clusters/ClusterView';

// /clusters — the photo-clustering plane (session 013): generate a run and browse
// the immutable cluster trees.
export default function ClustersPage() {
  return <ClusterView />;
}
