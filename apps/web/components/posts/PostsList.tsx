'use client';

import type { ReactNode } from 'react';

// Owner "My posts" listing (session 020) — fetches listPosts() on mount and links
// each post back to its editor (the summary has no slug). GREEN fills loading /
// error / empty / rows states.
export function PostsList(): ReactNode {
  throw new Error('not implemented');
}
