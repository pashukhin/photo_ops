
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local git worktrees
.worktrees/

node_modules/
.next/
dist/
coverage/
# ...but keep the diff-coverage tooling package (photo_ops-osq); re-ignore its venv
!scripts/coverage/
scripts/coverage/.venv/
*.tsbuildinfo
.env
.env.local
*.log
__pycache__/
*.egg-info/
.venv/
bin/
tmp/
data/

# subagent-driven-development scratch
.superpowers/

# Claude Code local (user/machine-specific) settings
.claude/settings.local.json

# Editor settings (user-specific)
.vscode/

# smoke-stack build/up/migrate logs (scripts/smoke-stack.sh)
.smoke-stack/
.smoke-stack-up.log

# Playwright UI-smoke artifacts (make smoke-ui)
test-results/
playwright-report/

# diff-based coverage report artifacts (make coverage-diff)
.coverage/
